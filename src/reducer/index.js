const initialState = {
    email: '',
    password: '',
    isLogging: false
}

export const reducer = (state = initialState, action) => {
    switch(action.type) {
       default:
       return state;

    }
}

